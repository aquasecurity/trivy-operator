---
apiVersion: batch/v1
kind: Job
metadata:
  name: pi
spec:
  template:
    spec:
      containers:
        - command: ["perl", "-Mbignum=bpi", "-wle", "'print bpi(2000)'"]
          image: perl:5.34
          name: pi
      restartPolicy: Never
