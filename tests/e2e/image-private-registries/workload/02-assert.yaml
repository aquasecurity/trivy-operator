---
apiVersion: kuttl.dev/v1beta1
commands:
  - script: >
      ttl="$(kubectl get -n trivy-system
      secrets scan-vulnerabilityreport-db9b87967-regcred \
        -o=jsonpath='{.metadata.annotations.trivy-operator\.aquasecurity\.github\.io/secret-ttl}')"
      if [ $ttl == "0s" ]

      then
        echo "Pass: scan job secret ttl ($ttl) match"
      else
        echo "Fail: scan job secret ttl ($ttl) do not match"
        exit 1
      fi
kind: TestAssert
timeout: 30
